extends ../layout

block content
  form(
    method=method
    action=action
  )
    label
      strong Name
      input(name="name" value=player.name)

    label
      strong Email
      input(name="email" value=player.email)

    label
      input(type="submit" value="Envoyer")